using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Bubblemancer : Body
{
    public int i = 0; //For some reason, Unity does not want to load empty child classes into the editor... So that's why this is here
    //Right now this is just the same as the normal body, but may change in the future
}
